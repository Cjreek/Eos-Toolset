<UserControl x:Class="Eos.Usercontrols.ModelResourceTextbox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Eos.Usercontrols"
             mc:Ignorable="d" 
             x:Name="ucModelResourceTextbox"
             d:DesignHeight="22" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>

        <TextBox x:Name="tbModel" Grid.Column="0" Text="{Binding ElementName=ucModelResourceTextbox, Path=ResRef}"/>
        <Button Grid.Column="1" Content="..." Margin="3,0,0,0" Height="{Binding ElementName=tbModel, Path=ActualHeight}" Width="{Binding ElementName=tbModel, Path=ActualHeight}"/>
    </Grid>
</UserControl>
