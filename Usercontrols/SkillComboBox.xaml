<UserControl x:Class="Eos.Usercontrols.SkillComboBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Eos.Usercontrols"
             xmlns:repo="clr-namespace:Eos.Models.Base"
             mc:Ignorable="d" 
             x:Name="ucSkillComboBox"
             d:DesignHeight="22" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>

        <ComboBox x:Name="cbSkills" Grid.Column="0" ItemsSource="{Binding Source={x:Static repo:MasterRepository.Skills}}" DisplayMemberPath="Name" SelectedValue="{Binding ElementName=ucSkillComboBox, Path=SelectedValue}"/>
        <Button x:Name="btClear" Grid.Column="1" Margin="2,-2,0,0" Height="{Binding ElementName=cbSkills, Path=ActualHeight}" Width="{Binding ElementName=cbSkills, Path=ActualHeight}" Content="X" Click="btClear_Click"/>
    </Grid>
</UserControl>
